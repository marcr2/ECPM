---
phase: 03-predictive-modeling-and-crisis-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/ecpm/modeling/__init__.py
  - backend/ecpm/modeling/schemas.py
  - backend/tests/conftest.py
  - backend/tests/test_stationarity.py
  - backend/tests/test_var_model.py
  - backend/tests/test_svar_model.py
  - backend/tests/test_regime_switching.py
  - backend/tests/test_crisis_index.py
  - backend/tests/test_backtest.py
  - backend/tests/test_api_forecasting.py
autonomous: true
requirements: [MODL-01, MODL-02, MODL-03, MODL-04, MODL-05, MODL-06, MODL-07, MODL-08, DASH-06]

must_haves:
  truths:
    - "All modeling test files exist with skip-marked tests that specify expected behavior"
    - "Pydantic schemas define the complete API contract for forecasts, regimes, backtests, crisis index, and training status"
    - "Synthetic time-series fixtures produce deterministic data suitable for testing VAR and regime-switching models"
    - "statsmodels>=0.14.6 is listed as a project dependency"
  artifacts:
    - path: "backend/ecpm/modeling/schemas.py"
      provides: "Pydantic v2 response schemas for all modeling API endpoints"
      exports: ["ForecastPoint", "IndicatorForecast", "RegimeResult", "CrisisIndex", "BacktestResult", "TrainingStatus", "TrainingStep"]
    - path: "backend/ecpm/modeling/__init__.py"
      provides: "Package marker and public exports"
    - path: "backend/tests/conftest.py"
      provides: "Synthetic indicator fixtures for modeling tests"
      contains: "synthetic_indicators"
    - path: "backend/tests/test_stationarity.py"
      provides: "Test scaffold for MODL-02"
    - path: "backend/tests/test_var_model.py"
      provides: "Test scaffold for MODL-01, MODL-04"
    - path: "backend/tests/test_svar_model.py"
      provides: "Test scaffold for MODL-03"
    - path: "backend/tests/test_regime_switching.py"
      provides: "Test scaffold for MODL-06"
    - path: "backend/tests/test_crisis_index.py"
      provides: "Test scaffold for MODL-07, MODL-08"
    - path: "backend/tests/test_backtest.py"
      provides: "Test scaffold for MODL-05"
    - path: "backend/tests/test_api_forecasting.py"
      provides: "Test scaffold for DASH-06"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "all test_*.py files"
      via: "synthetic_indicators fixture"
      pattern: "synthetic_indicators"
    - from: "backend/tests/test_api_forecasting.py"
      to: "backend/ecpm/modeling/schemas.py"
      via: "Pydantic schema imports for response validation"
      pattern: "from ecpm.modeling.schemas import"
---

<objective>
Add statsmodels dependency, create the modeling package with Pydantic response schemas, and scaffold all test files with synthetic data fixtures for the Phase 3 modeling pipeline.

Purpose: Establish the test infrastructure and API contracts before any modeling implementation. Test scaffolds serve as executable specifications (Nyquist compliance). Schemas define the contract between backend modeling code and both API endpoints and frontend consumers.

Output: modeling/ package stub, 7 test scaffold files with skip-marked tests, synthetic time-series conftest fixtures, Pydantic schemas for all forecast/regime/backtest/training responses.
</objective>

<execution_context>
@/home/marcellinor/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcellinor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-predictive-modeling-and-crisis-index/03-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From backend/ecpm/indicators/definitions.py:
```python
class IndicatorSlug(StrEnum):
    rate_of_profit = "rate_of_profit"
    occ = "occ"
    rate_of_surplus_value = "rate_of_surplus_value"
    mass_of_profit = "mass_of_profit"
    productivity_wage_gap = "productivity_wage_gap"
    credit_gdp_gap = "credit_gdp_gap"
    financial_real_ratio = "financial_real_ratio"
    debt_service_ratio = "debt_service_ratio"
```

From backend/ecpm/schemas/series.py (pattern to follow):
```python
from pydantic import BaseModel, ConfigDict
# Response schemas use model_validate and ConfigDict
```

From backend/tests/conftest.py (existing fixture pattern):
```python
# Uses @pytest.fixture, pytest.importorskip, aiosqlite async session
# Dict-backed mock Redis, in-memory SQLite
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add statsmodels dependency and create modeling package with Pydantic schemas</name>
  <files>backend/pyproject.toml, backend/ecpm/modeling/__init__.py, backend/ecpm/modeling/schemas.py</files>
  <action>
1. Add `"statsmodels>=0.14.6"` to the `dependencies` list in `backend/pyproject.toml`.

2. Create `backend/ecpm/modeling/__init__.py` as an empty package marker with a docstring: "Econometric modeling pipeline: VAR/SVAR forecasting, regime-switching, crisis index."

3. Create `backend/ecpm/modeling/schemas.py` with Pydantic v2 response models following the existing pattern in `ecpm/schemas/series.py`. Define these models:

- `TrainingStep(BaseModel)`: name (str), status (str: "pending"|"running"|"complete"|"error"), duration_ms (int|None), detail (str|None)
- `TrainingStatus(BaseModel)`: task_id (str), status (str: "idle"|"running"|"complete"|"error"), current_step (str), steps (list[TrainingStep]), started_at (str|None), completed_at (str|None), error (str|None)
- `ForecastPoint(BaseModel)`: date (str), point (float), lower_68 (float), upper_68 (float), lower_95 (float), upper_95 (float)
- `IndicatorForecast(BaseModel)`: indicator (str), horizon_quarters (int), forecasts (list[ForecastPoint]), model_info (dict) -- model_info includes lag_order, aic, differencing_order
- `RegimeResult(BaseModel)`: current_regime (int), regime_label (str), regime_probabilities (dict[str,float]), transition_matrix (list[list[float]]), smoothed_probabilities (list[dict]), regime_durations (dict[str,float]|None)
- `CrisisIndex(BaseModel)`: current_value (float), trpf_component (float), realization_component (float), financial_component (float), history (list[dict]) -- history entries: {date, composite, trpf, realization, financial}
- `BacktestResult(BaseModel)`: episode_name (str), start_date (str), end_date (str), crisis_index_series (list[dict]), warning_12m (bool), warning_24m (bool), peak_value (float), peak_date (str)
- `ForecastsResponse(BaseModel)`: forecasts (dict[str, IndicatorForecast]), generated_at (str)
- `BacktestsResponse(BaseModel)`: backtests (list[BacktestResult]), generated_at (str)

All models should use `model_config = ConfigDict(from_attributes=True)` where needed.
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM/backend && python -c "from ecpm.modeling.schemas import ForecastPoint, IndicatorForecast, RegimeResult, CrisisIndex, BacktestResult, TrainingStatus, ForecastsResponse, BacktestsResponse; print('All schemas importable')"</automated>
  </verify>
  <done>statsmodels in pyproject.toml dependencies, modeling package created, all 9 Pydantic response schemas importable with correct fields</done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffolds with synthetic data fixtures for all modeling modules</name>
  <files>backend/tests/conftest.py, backend/tests/test_stationarity.py, backend/tests/test_var_model.py, backend/tests/test_svar_model.py, backend/tests/test_regime_switching.py, backend/tests/test_crisis_index.py, backend/tests/test_backtest.py, backend/tests/test_api_forecasting.py</files>
  <action>
1. Add to `backend/tests/conftest.py` a new fixture `synthetic_indicators` that generates a pandas DataFrame with 8 columns (one per IndicatorSlug) and ~120 rows (30 years of quarterly data, 1993Q1 to 2023Q4) using numpy random with a fixed seed (42) for deterministic results. The data should:
   - Use `pd.date_range("1993-01-01", periods=124, freq="QS")` as the DatetimeIndex
   - Generate realistic-looking economic time series: trending with noise, some with structural breaks
   - rate_of_profit: declining trend with noise (start ~0.15, end ~0.08)
   - occ: rising trend (start ~3.0, end ~5.5)
   - rate_of_surplus_value: gently rising (start ~1.5, end ~2.0)
   - mass_of_profit: strongly rising (start ~500, end ~2000)
   - productivity_wage_gap: rising (start ~100, end ~160)
   - credit_gdp_gap: oscillating around 0 with structural breaks (range -10 to +15)
   - financial_real_ratio: rising (start ~1.2, end ~3.5)
   - debt_service_ratio: oscillating (range 10-20%)
   Also add a `synthetic_regime_series` fixture: a pd.Series with 3 distinct regimes (low-vol normal, medium-vol stagnation, high-vol crisis periods) for regime-switching tests.

2. Create `backend/tests/test_stationarity.py` with skip-marked tests:
   - `test_dual_stationarity_stationary_series`: Passes a stationary series, expects both ADF and KPSS to agree
   - `test_dual_stationarity_nonstationary_series`: Passes a random-walk, expects non-stationary detection
   - `test_ensure_stationarity_differences_when_needed`: Passes mixed-stationarity DataFrame, expects differenced output with correct diff_orders
   - `test_ensure_stationarity_preserves_stationary`: Passes stationary DataFrame, expects no differencing (diff_orders all 0)
   Use `pytest.importorskip("ecpm.modeling.stationarity")` pattern.

3. Create `backend/tests/test_var_model.py` with skip-marked tests:
   - `test_var_lag_selection`: Fit VAR on synthetic data, verify selected lag is reasonable (1-12)
   - `test_var_forecast_shape`: Verify forecast output has correct shape (steps x n_vars)
   - `test_forecast_confidence_intervals`: Verify 68% CI is narrower than 95% CI for all variables and all horizons
   - `test_forecast_includes_all_indicators`: Verify forecasts dict has keys for all 8 indicators
   Use `pytest.importorskip("ecpm.modeling.var_model")`.

4. Create `backend/tests/test_svar_model.py` with skip-marked tests:
   - `test_svar_a_matrix_shape`: Verify A matrix is (8,8) with 1s on diagonal
   - `test_svar_a_matrix_lower_triangular`: Verify upper triangle is all zeros, lower has 'E' markers
   - `test_svar_identification`: Fit SVAR on synthetic data, verify it produces results without error
   Use `pytest.importorskip("ecpm.modeling.svar_model")`.

5. Create `backend/tests/test_regime_switching.py` with skip-marked tests:
   - `test_regime_detection`: Fit regime model on synthetic_regime_series, verify it identifies multiple regimes
   - `test_regime_probabilities_sum_to_one`: Verify smoothed probabilities sum to ~1.0 at each time point
   - `test_transition_matrix_rows_sum_to_one`: Verify each row of transition matrix sums to ~1.0
   - `test_regime_fallback_to_2_regimes`: If 3-regime fails, verify fallback to 2 regimes works
   Use `pytest.importorskip("ecpm.modeling.regime_switching")`.

6. Create `backend/tests/test_crisis_index.py` with skip-marked tests:
   - `test_composite_index`: Verify composite index is in [0, 100] range
   - `test_mechanism_decomposition`: Verify trpf, realization, financial components are returned separately
   - `test_equal_weights_default`: Verify default weights are 1/3 each
   - `test_custom_weights`: Verify custom weights change the composite index value
   - `test_inverted_profit_rate`: Verify lower profit rate produces higher TRPF crisis signal
   Use `pytest.importorskip("ecpm.modeling.crisis_index")`.

7. Create `backend/tests/test_backtest.py` with skip-marked tests:
   - `test_gfc_backtest`: Run backtest for GFC episode, verify result structure has all required fields
   - `test_backtest_result_structure`: Verify BacktestResult schema validation passes
   - `test_backtest_crisis_index_series`: Verify crisis_index_series has entries for the episode time range
   Use `pytest.importorskip("ecpm.modeling.backtest")`.

8. Create `backend/tests/test_api_forecasting.py` with skip-marked tests:
   - `test_get_forecasts`: GET /api/forecasting/forecasts returns 200 with ForecastsResponse structure
   - `test_get_regime`: GET /api/forecasting/regime returns 200 with RegimeResult structure
   - `test_get_crisis_index`: GET /api/forecasting/crisis-index returns 200 with CrisisIndex structure
   - `test_get_backtests`: GET /api/forecasting/backtests returns 200 with BacktestsResponse structure
   - `test_trigger_training`: POST /api/forecasting/train returns 202 with task_id
   Use `pytest.importorskip("ecpm.api.forecasting")` and the existing API test pattern from test_api.py.

All test files follow the existing project pattern: `pytest.mark.skipif` or `pytest.importorskip` for modules not yet implemented, concrete assertions that serve as specifications.
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM/backend && python -m pytest tests/test_stationarity.py tests/test_var_model.py tests/test_svar_model.py tests/test_regime_switching.py tests/test_crisis_index.py tests/test_backtest.py tests/test_api_forecasting.py -v --timeout=30 2>&1 | tail -20</automated>
  </verify>
  <done>7 new test files exist with skip-marked tests (all skipped since implementation modules do not exist yet). Synthetic indicator fixtures in conftest.py produce deterministic 8-column DataFrame. Existing tests still pass.</done>
</task>

</tasks>

<verification>
- `python -c "from ecpm.modeling.schemas import ..."` succeeds for all schema classes
- `python -m pytest tests/ -x -q --timeout=30` passes (existing tests unbroken, new tests all skipped)
- `grep statsmodels backend/pyproject.toml` shows the dependency
- All 7 new test files exist and contain skip-marked test functions with concrete assertions
</verification>

<success_criteria>
- statsmodels>=0.14.6 added to pyproject.toml
- modeling/ package created with __init__.py and schemas.py
- 9 Pydantic response schemas defined and importable
- 7 test scaffold files with ~30 skip-marked tests total
- Synthetic indicator fixtures in conftest.py (deterministic, 124 quarters, 8 columns)
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-predictive-modeling-and-crisis-index/03-01-SUMMARY.md`
</output>
