---
phase: 01-foundation-and-data-ingestion
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_fred_client.py
  - backend/tests/test_bea_client.py
  - backend/tests/test_ingestion_pipeline.py
  - backend/tests/test_models.py
  - backend/tests/test_tasks.py
  - backend/tests/test_api.py
  - backend/tests/test_cache.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - DATA-08
  - DATA-09
  - INFR-02
  - INFR-03
  - INFR-04

must_haves:
  truths:
    - "All test files exist and are importable"
    - "pytest discovers all test files and reports them as collected (even if tests fail due to missing implementation)"
    - "conftest.py provides async session fixtures, mock API clients, and test database helpers"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Shared test fixtures (async session, mock FRED/BEA clients, test DB)"
      min_lines: 30
    - path: "backend/tests/test_fred_client.py"
      provides: "Tests for FRED client auth, retry backoff"
      contains: "test_retry_backoff"
    - path: "backend/tests/test_bea_client.py"
      provides: "Tests for BEA client NIPA/FixedAssets retrieval"
    - path: "backend/tests/test_ingestion_pipeline.py"
      provides: "Tests for pipeline persistence, gap handling, native frequency"
      contains: "test_gap_handling.*test_native_frequency"
    - path: "backend/tests/test_models.py"
      provides: "Tests for metadata fields, vintage_date column"
      contains: "test_metadata_fields.*test_vintage_date_column"
    - path: "backend/tests/test_tasks.py"
      provides: "Tests for Celery scheduled fetch"
      contains: "test_scheduled_fetch"
    - path: "backend/tests/test_api.py"
      provides: "Tests for REST endpoints and SSE stream"
      contains: "test_sse_stream"
    - path: "backend/tests/test_cache.py"
      provides: "Tests for Redis caching"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "backend/ecpm/database.py"
      via: "test session factory import"
      pattern: "async_session|get_db"
---

<objective>
Create all test infrastructure files (Wave 0) so that subsequent implementation plans can use pytest-based verification commands. Tests will initially fail (RED state) since production code does not exist yet -- that is expected and correct.

Purpose: Satisfy Nyquist compliance -- every plan's verify step must reference an automated test command, and those test files must exist before execution begins.
Output: 9 test files plus conftest.py with shared fixtures.
</objective>

<execution_context>
@/home/marcellinor/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcellinor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-ingestion/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-ingestion/01-RESEARCH.md
@.planning/phases/01-foundation-and-data-ingestion/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixtures and all test scaffold files</name>
  <files>
    backend/tests/__init__.py,
    backend/tests/conftest.py,
    backend/tests/test_fred_client.py,
    backend/tests/test_bea_client.py,
    backend/tests/test_ingestion_pipeline.py,
    backend/tests/test_models.py,
    backend/tests/test_tasks.py,
    backend/tests/test_api.py,
    backend/tests/test_cache.py
  </files>
  <action>
    Create all test infrastructure for Phase 1. Tests should be written as real test cases with assertions -- they will fail (RED) until production code is implemented in subsequent plans. Use `pytest.importorskip` or try/except ImportError with `pytest.skip` for imports of modules that do not yet exist, so pytest can collect and report them cleanly.

    1. **backend/tests/__init__.py** -- Empty init.

    2. **backend/tests/conftest.py** -- Shared fixtures:
       - `@pytest.fixture` for mock FRED API client (returns canned pandas Series + info dict for GDPC1)
       - `@pytest.fixture` for mock BEA API client (returns canned DataFrame for NIPA T11200)
       - `@pytest.fixture` for async SQLAlchemy test session (uses sqlite+aiosqlite for unit tests, or skip if aiosqlite not available)
       - `@pytest.fixture` for test series_config dict (minimal: 2 FRED series, 1 BEA table)
       - `@pytest.fixture` for FastAPI TestClient (httpx.AsyncClient with app)
       - `@pytest.fixture` for mock Redis client (fakeredis or simple dict-based mock)
       - Import guards: wrap production code imports in try/except so conftest loads even before production code exists

    3. **backend/tests/test_fred_client.py** -- Tests for DATA-01, DATA-08:
       - `test_fred_client_init`: FredClient accepts api_key parameter
       - `test_fetch_series_returns_data`: fetch_series_sync returns (DataFrame, dict) tuple
       - `test_retry_backoff`: Mock fredapi to raise ConnectionError, verify tenacity retries (check retry count or wait behavior using unittest.mock)
       - `test_rate_limit_delay`: Verify 0.6s sleep exists between calls

    4. **backend/tests/test_bea_client.py** -- Tests for DATA-03:
       - `test_bea_client_init`: BEAClient accepts api_key parameter
       - `test_fetch_nipa_table`: Returns DataFrame with expected columns (LineNumber, DataValue, TimePeriod)
       - `test_fetch_fixed_assets`: Calls with datasetname='FixedAssets' (not NIPA)
       - `test_retry_on_error`: Verify tenacity retry decorator is configured

    5. **backend/tests/test_ingestion_pipeline.py** -- Tests for DATA-02, DATA-06, DATA-07:
       - `test_fred_persistence`: Pipeline stores FRED data in observations table
       - `test_bea_persistence`: Pipeline stores BEA data in observations table
       - `test_gap_handling`: NaN values stored as NULL with gap_flag=True, no interpolation
       - `test_native_frequency`: Monthly data stored as monthly, quarterly as quarterly (no resampling)
       - `test_error_continues`: Single series error does not abort entire pipeline
       - `test_metadata_upsert`: SeriesMetadata record created/updated with correct fields

    6. **backend/tests/test_models.py** -- Tests for DATA-04, DATA-09:
       - `test_metadata_fields`: SeriesMetadata model has all required columns (units, frequency, seasonal_adjustment, source, last_updated, observation_count, fetch_status)
       - `test_vintage_date_column`: Observation model has vintage_date column (nullable)
       - `test_gap_flag_column`: Observation model has gap_flag boolean column
       - `test_series_id_unique`: SeriesMetadata.series_id has unique constraint

    7. **backend/tests/test_tasks.py** -- Tests for DATA-05:
       - `test_celery_app_exists`: celery_app is a Celery instance
       - `test_scheduled_fetch`: beat_schedule contains "daily-data-refresh" key
       - `test_fetch_task_registered`: fetch_all_series is a registered Celery task

    8. **backend/tests/test_api.py** -- Tests for INFR-02, INFR-03:
       - `test_health_endpoint`: GET /health returns 200 with {"status": "healthy"}
       - `test_series_list_endpoint`: GET /api/data/series returns 200 with series list
       - `test_series_detail_endpoint`: GET /api/data/series/GDPC1 returns 200 with observations
       - `test_status_endpoint`: GET /api/data/status returns 200 with status fields
       - `test_fetch_trigger`: POST /api/data/fetch returns 200 with task_id
       - `test_sse_stream`: GET /api/data/fetch/stream returns SSE content type

    9. **backend/tests/test_cache.py** -- Tests for INFR-04:
       - `test_cache_set_get`: cache_set stores value, cache_get retrieves it
       - `test_cache_ttl`: Cached value expires after TTL
       - `test_cache_key_format`: Cache keys follow ecpm:api:{endpoint}:{hash} pattern

    After creating all files, update VALIDATION.md frontmatter: set `nyquist_compliant: true` and `wave_0_complete: true`.
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM && python -m pytest backend/tests/ --collect-only 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 9 test files exist under backend/tests/. pytest --collect-only discovers all test functions (they may show as errors or skips if production code is missing, but they are collected). conftest.py provides shared fixtures. VALIDATION.md updated with nyquist_compliant: true.
  </done>
</task>

</tasks>

<verification>
1. `ls backend/tests/test_*.py | wc -l` returns 7 (fred_client, bea_client, ingestion_pipeline, models, tasks, api, cache)
2. `python -m pytest backend/tests/ --collect-only` discovers test functions
3. conftest.py contains fixture definitions for mock clients and test session
</verification>

<success_criteria>
- All 9 test files exist (conftest + 7 test modules)
- pytest discovers all test functions
- Fixtures provide mock FRED/BEA clients, test DB session, test config
- VALIDATION.md frontmatter updated to nyquist_compliant: true
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-ingestion/01-00-SUMMARY.md`
</output>
