---
phase: 01-foundation-and-data-ingestion
plan: 02
type: execute
wave: 1
depends_on: ["01-00"]
files_modified:
  - backend/ecpm/__init__.py
  - backend/ecpm/__main__.py
  - backend/ecpm/config.py
  - backend/ecpm/database.py
  - backend/ecpm/main.py
  - backend/ecpm/models/__init__.py
  - backend/ecpm/models/observation.py
  - backend/ecpm/models/series_metadata.py
  - backend/ecpm/core/__init__.py
  - backend/ecpm/core/logging.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/versions/001_initial_schema.py
  - backend/series_config.yaml
autonomous: true
requirements:
  - DATA-04
  - DATA-07
  - DATA-09

must_haves:
  truths:
    - "TimescaleDB contains series_metadata table and observations hypertable with correct columns"
    - "observations table stores data at native frequency with vintage_date column"
    - "series_metadata table stores units, frequency, seasonality, source, last_updated, observation_count, fetch_status"
    - "Backend Python package is importable and uvicorn serves a /health endpoint"
  artifacts:
    - path: "backend/ecpm/models/observation.py"
      provides: "Observations hypertable model"
      contains: "vintage_date"
    - path: "backend/ecpm/models/series_metadata.py"
      provides: "Series metadata model"
      contains: "frequency.*units.*fetch_status"
    - path: "backend/ecpm/database.py"
      provides: "Async SQLAlchemy engine and session factory"
      exports: ["engine", "async_session", "get_db"]
    - path: "backend/series_config.yaml"
      provides: "FRED and BEA series definitions"
    - path: "backend/alembic/versions/001_initial_schema.py"
      provides: "Initial database migration"
    - path: "backend/ecpm/config.py"
      provides: "Pydantic Settings for env vars"
      exports: ["Settings", "get_settings"]
  key_links:
    - from: "backend/ecpm/database.py"
      to: "docker-compose.yml timescaledb service"
      via: "postgresql+asyncpg connection string"
      pattern: "postgresql\\+asyncpg"
    - from: "backend/alembic/env.py"
      to: "backend/ecpm/models"
      via: "target_metadata import"
      pattern: "target_metadata"
---

<objective>
Create the Python backend package structure: config, database, models, Alembic migrations, structured logging, CLI stub, and series configuration YAML. This is the code foundation that ingestion and API plans build upon.

Purpose: Define the data models and database layer that all backend functionality depends on.
Output: Importable Python package with SQLAlchemy models, Alembic migration, FastAPI health endpoint, series config.
</objective>

<execution_context>
@/home/marcellinor/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcellinor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-ingestion/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-ingestion/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend package structure, config, database, and logging</name>
  <files>
    backend/ecpm/__init__.py,
    backend/ecpm/__main__.py,
    backend/ecpm/config.py,
    backend/ecpm/database.py,
    backend/ecpm/main.py,
    backend/ecpm/core/__init__.py,
    backend/ecpm/core/logging.py
  </files>
  <action>
    1. **backend/ecpm/__init__.py** -- Package init with version: `__version__ = "0.1.0"`

    2. **backend/ecpm/config.py** -- Pydantic Settings class `Settings`:
       - database_url, redis_url, fred_api_key, bea_api_key
       - celery_broker_url, celery_result_backend
       - fetch_schedule_hour (default 6), fetch_schedule_minute (default 0)
       - log_level (default "INFO")
       - Use `model_config = SettingsConfigDict(env_file=".env")` (Pydantic v2 style)
       - Singleton `get_settings()` function with lru_cache

    3. **backend/ecpm/database.py** -- Async SQLAlchemy 2.0 setup:
       - `create_async_engine` with pool_size=5, max_overflow=10
       - `async_sessionmaker` with `class_=AsyncSession, expire_on_commit=False`
       - `get_db()` async generator for FastAPI dependency injection (yield session, commit on success, rollback on exception)

    4. **backend/ecpm/main.py** -- Minimal FastAPI app:
       - `app = FastAPI(title="ECPM", version="0.1.0")`
       - `/health` endpoint returning `{"status": "healthy"}`
       - CORS middleware allowing localhost:3000
       - Lifespan handler that tests DB connection on startup

    5. **backend/ecpm/core/__init__.py** -- Empty init.

    6. **backend/ecpm/core/logging.py** -- structlog configuration:
       - JSON renderer for production, console renderer for dev (based on LOG_LEVEL)
       - Add timestamp, log level, caller info processors
       - `setup_logging()` function called from main.py lifespan

    7. **backend/ecpm/__main__.py** -- CLI entry point stub:
       - `python -m ecpm fetch` placeholder (argparse with "fetch" subcommand, prints "Not implemented yet")
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM && python -c "import sys; sys.path.insert(0, 'backend'); from ecpm.config import Settings, get_settings; from ecpm.database import get_db; from ecpm.main import app; print('Package OK')" 2>&1</automated>
  </verify>
  <done>
    Backend package importable. Settings loads from env. Database module provides async session factory. FastAPI app serves /health. structlog configured. CLI stub responds to `python -m ecpm fetch`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create database models, Alembic migration, and series config</name>
  <files>
    backend/ecpm/models/__init__.py,
    backend/ecpm/models/observation.py,
    backend/ecpm/models/series_metadata.py,
    backend/alembic.ini,
    backend/alembic/env.py,
    backend/alembic/versions/001_initial_schema.py,
    backend/series_config.yaml
  </files>
  <action>
    1. **backend/ecpm/models/__init__.py** -- Import and re-export all models. Define `Base` using SQLAlchemy 2.0 `DeclarativeBase`:
       ```python
       from sqlalchemy.orm import DeclarativeBase
       class Base(DeclarativeBase):
           pass
       ```
       Import SeriesMetadata and Observation so Alembic can discover them.

    2. **backend/ecpm/models/series_metadata.py** -- SQLAlchemy 2.0 mapped model:
       - Table: `series_metadata`
       - Columns (use `Mapped` type annotations):
         - `id`: Integer, primary_key, autoincrement
         - `series_id`: String(50), unique, not null
         - `source`: String(10), not null -- "FRED" or "BEA"
         - `source_detail`: JSON (nullable)
         - `name`: Text, not null
         - `units`: String(100), nullable
         - `frequency`: String(1), not null -- "D", "M", "Q", "A"
         - `seasonal_adjustment`: String(50), nullable
         - `last_updated`: DateTime(timezone=True), nullable
         - `last_fetched`: DateTime(timezone=True), nullable
         - `observation_count`: Integer, default 0
         - `fetch_status`: String(20), default "pending"
         - `fetch_error`: Text, nullable
         - `created_at`: DateTime(timezone=True), server_default=func.now()

    3. **backend/ecpm/models/observation.py** -- SQLAlchemy 2.0 mapped model:
       - Table: `observations`
       - Columns:
         - `observation_date`: DateTime(timezone=True), primary_key=True
         - `series_id`: String(50), ForeignKey("series_metadata.series_id"), primary_key=True
         - `value`: Float, nullable
         - `vintage_date`: Date, nullable -- DATA-09
         - `gap_flag`: Boolean, default False
         - `created_at`: DateTime(timezone=True), server_default=func.now()

    4. **backend/alembic.ini** -- Standard config pointing to `alembic/` directory

    5. **backend/alembic/env.py** -- Async migration support:
       - Import `Base.metadata` as `target_metadata`
       - `include_object` filter to exclude TimescaleDB internal tables/indexes
       - Async engine and `run_sync` pattern

    6. **backend/alembic/versions/001_initial_schema.py** -- Initial migration:
       - Create `series_metadata` and `observations` tables
       - Execute raw SQL: `SELECT create_hypertable('observations', 'observation_date', ...)`
       - Create indexes including partial unique indexes for vintage_date
       - Downgrade: drop both tables

    7. **backend/series_config.yaml** -- Full series configuration per user decisions:
       - `fred` section: ~50-80 FRED series with `id`, `name`, `category` fields
       - `bea.nipa` section: Tables T11200, T61900, T10300, T11500
       - `bea.fixed_assets` section: FAAt101
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM && python -c "import sys; sys.path.insert(0, 'backend'); from ecpm.models import Base, SeriesMetadata, Observation; cols_meta = [c.name for c in SeriesMetadata.__table__.columns]; cols_obs = [c.name for c in Observation.__table__.columns]; assert 'vintage_date' in cols_obs, 'missing vintage_date'; assert 'fetch_status' in cols_meta, 'missing fetch_status'; assert 'frequency' in cols_meta, 'missing frequency'; print('Models OK:', len(Base.metadata.tables), 'tables'); print('Metadata cols:', cols_meta); print('Observation cols:', cols_obs)" 2>&1</automated>
  </verify>
  <done>
    SQLAlchemy models define series_metadata and observations tables with all required columns. Alembic migration creates both tables, hypertable, and indexes. series_config.yaml contains 50+ FRED series and all required BEA tables. vintage_date present (DATA-09). Native frequency stored (DATA-07). Metadata covers all DATA-04 fields.
  </done>
</task>

</tasks>

<verification>
1. Python package imports successfully
2. Models have all required columns (verified programmatically)
3. series_config.yaml is valid YAML with fred and bea sections
4. Alembic env.py has include_object filter for TimescaleDB
</verification>

<success_criteria>
- SQLAlchemy 2.0 models define series_metadata + observations with all required columns (DATA-04, DATA-07, DATA-09)
- Alembic migration creates hypertable and indexes
- series_config.yaml has 50+ FRED series and all required BEA tables
- Backend package structure matches research architecture
- structlog configured for JSON logging
- Pydantic Settings loads all env vars
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-ingestion/01-02-SUMMARY.md`
</output>
