---
phase: 01-foundation-and-data-ingestion
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/ecpm/ingestion/pipeline.py
  - backend/tests/conftest.py
  - backend/tests/test_ingestion_pipeline.py
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [DATA-01, DATA-04, DATA-07, DATA-08, DATA-09]

must_haves:
  truths:
    - "IngestionPipeline.ingest_fred_series() calls FredClient.fetch_series() which exercises the tenacity retry decorator"
    - "Test mock_fred_client fixture matches FredClient's real interface (fetch_series returns (data, info) tuple)"
    - "All existing pipeline tests pass against the corrected code path"
    - "REQUIREMENTS.md checkboxes and traceability table are accurate for all Phase 1 requirements"
  artifacts:
    - path: "backend/ecpm/ingestion/pipeline.py"
      provides: "Corrected FRED ingestion calling fetch_series()"
      contains: "fred_client.fetch_series"
    - path: "backend/tests/conftest.py"
      provides: "Mock matching real FredClient interface"
      contains: "fetch_series"
    - path: "backend/tests/test_ingestion_pipeline.py"
      provides: "Tests using fetch_series interface"
      contains: "fetch_series"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement tracking for Phase 1"
      contains: "[x] **DATA-04**"
  key_links:
    - from: "backend/ecpm/ingestion/pipeline.py"
      to: "backend/ecpm/ingestion/fred_client.py"
      via: "fred_client.fetch_series(series_id) call"
      pattern: "fred_client\\.fetch_series"
    - from: "backend/tests/conftest.py"
      to: "backend/ecpm/ingestion/fred_client.py"
      via: "mock_fred_client fixture matching FredClient.fetch_series signature"
      pattern: "fetch_series\\.return_value"
---

<objective>
Close 2 verification gaps from 01-VERIFICATION.md:

1. **Critical pipeline wiring bug:** `pipeline.py` calls `fred_client.get_series()` and `fred_client.get_series_info()` -- methods that do NOT exist on `FredClient`. Must call `fred_client.fetch_series(series_id)` which returns a `(data, info)` tuple and exercises the tenacity retry decorator.

2. **Stale REQUIREMENTS.md:** DATA-04, DATA-07, DATA-09 are fully implemented but still marked `[ ] Pending` in checkboxes and traceability table.

Purpose: Fix the only BLOCKER preventing Phase 1 from being truly complete. Without this fix, the FRED ingestion pipeline will raise `AttributeError` at runtime with a real `FredClient`, and the exponential backoff retry logic (DATA-01, DATA-08) is never exercised.

Output: Corrected pipeline code, aligned test mocks, accurate requirements tracking.
</objective>

<execution_context>
@/home/marcellinor/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcellinor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-ingestion/01-VERIFICATION.md

<interfaces>
<!-- FredClient's real public interface (from backend/ecpm/ingestion/fred_client.py): -->

```python
class FredClient:
    def __init__(self, api_key: str) -> None: ...

    def fetch_series(self, series_id: str) -> tuple[pd.Series, dict[str, Any]]:
        """Returns (pandas Series of observations, dict of series metadata)."""
        ...

    async def fetch_series_async(self, series_id: str) -> tuple[pd.Series, dict[str, Any]]:
        """Async version via run_in_executor."""
        ...
```

<!-- Current BROKEN pipeline code (backend/ecpm/ingestion/pipeline.py lines 295-297): -->

```python
# CURRENT (broken with real FredClient):
data = self.fred_client.get_series(series_id)
info_raw = self.fred_client.get_series_info(series_id)
```

<!-- Current BROKEN conftest mock (backend/tests/conftest.py lines 125-126): -->

```python
# CURRENT (matches broken pipeline, not real FredClient):
client.get_series.return_value = gdpc1_data
client.get_series_info.return_value = gdpc1_info
```

<!-- Test files that call mock_fred_client.get_series directly (must also update): -->
<!-- backend/tests/test_ingestion_pipeline.py lines 116, 166, 200-209 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pipeline-to-FredClient wiring and align test mocks</name>
  <files>
    backend/ecpm/ingestion/pipeline.py,
    backend/tests/conftest.py,
    backend/tests/test_ingestion_pipeline.py
  </files>
  <action>
Fix the critical wiring bug where pipeline.py calls nonexistent methods on FredClient, and align all test mocks to match the real FredClient interface.

**In `backend/ecpm/ingestion/pipeline.py` (lines 295-297):**
Replace:
```python
data = self.fred_client.get_series(series_id)
info_raw = self.fred_client.get_series_info(series_id)
```
With:
```python
data, info_raw = self.fred_client.fetch_series(series_id)
```
Also remove the comment on line 295 that says "matching conftest mock interface" -- it was the wrong interface. The info dict conversion logic below (lines 299-305) stays as-is since `fetch_series()` returns the info as a dict already, but the isinstance check is harmless and provides safety.

**In `backend/tests/conftest.py` (mock_fred_client fixture, lines 93-127):**
Update the mock to expose `fetch_series` returning a `(data, info)` tuple instead of separate `get_series` and `get_series_info` methods:
```python
client.fetch_series.return_value = (gdpc1_data, gdpc1_info)
```
Remove the `client.get_series.return_value` and `client.get_series_info.return_value` lines. Update the docstring to document the new interface: "client.fetch_series(series_id) -> (pandas Series, dict)".

**In `backend/tests/test_ingestion_pipeline.py`:**
- `TestGapHandling.test_gap_handling` (line 116): Replace `mock_fred_client.get_series.return_value = data_with_gaps` with `mock_fred_client.fetch_series.return_value = (data_with_gaps, {"id": "GDPC1", "title": "Real GDP", "units": "Billions", "frequency": "Quarterly", "seasonal_adjustment": "SA"})`.
- `TestNativeFrequency.test_native_frequency` (line 166): Replace `mock_fred_client.get_series.return_value = monthly_data` with `mock_fred_client.fetch_series.return_value = (monthly_data, {"id": "UNRATE", "title": "Unemployment Rate", "units": "Percent", "frequency": "Monthly", "seasonal_adjustment": "SA"})`.
- `TestErrorContinues.test_error_continues` (lines 200-209): Replace `mock_fred_client.get_series.side_effect` logic with `mock_fred_client.fetch_series.side_effect` logic. The `conditional_fail` function should raise `ConnectionError` for `series_id == "GDPC1"` and return the mock's `fetch_series.return_value` (which is the `(data, info)` tuple from the fixture) otherwise. Update `call_count` tracking to use `fetch_series`.

Do NOT modify `FredClient` itself -- the production client is correct. Only fix the pipeline caller and test mocks.
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM/backend && python -m pytest tests/test_ingestion_pipeline.py tests/test_fred_client.py -x -v --timeout=30</automated>
  </verify>
  <done>
    - pipeline.py calls fred_client.fetch_series(series_id) instead of nonexistent methods
    - conftest mock_fred_client exposes fetch_series returning (data, info) tuple
    - All 6 pipeline tests pass (fred_persistence, bea_persistence, gap_handling, native_frequency, error_continues, metadata_upsert)
    - All 5 fred_client tests still pass
    - grep for "get_series" in pipeline.py returns 0 matches (no references to the old broken interface)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes and traceability for DATA-04, DATA-07, DATA-09</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update three requirement entries that are fully implemented but still marked Pending:

1. **DATA-04** (line 15): Change `- [ ] **DATA-04**` to `- [x] **DATA-04**`
2. **DATA-07** (line 18): Change `- [ ] **DATA-07**` to `- [x] **DATA-07**`
3. **DATA-09** (line 20): Change `- [ ] **DATA-09**` to `- [x] **DATA-09**`

4. **Traceability table** (lines ~130, 133, 135): Change Status from `Pending` to `Complete` for DATA-04, DATA-07, DATA-09.

Do NOT change any other requirements or traceability rows.
  </action>
  <verify>
    <automated>grep -n "DATA-04\|DATA-07\|DATA-09" /home/marcellinor/Desktop/ECPM/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - DATA-04, DATA-07, DATA-09 checkboxes show [x] (checked)
    - Traceability rows for DATA-04, DATA-07, DATA-09 show "Complete"
    - All other requirements unchanged
  </done>
</task>

</tasks>

<verification>
1. All pipeline tests pass: `cd backend && python -m pytest tests/test_ingestion_pipeline.py -x -v`
2. All FRED client tests pass: `cd backend && python -m pytest tests/test_fred_client.py -x -v`
3. Full backend test suite passes: `cd backend && python -m pytest tests/ -x -v --timeout=30`
4. No references to `get_series` or `get_series_info` in pipeline.py: `grep -c "get_series" backend/ecpm/ingestion/pipeline.py` returns 0
5. REQUIREMENTS.md shows all Phase 1 requirements as [x] Complete
</verification>

<success_criteria>
- pipeline.py calls fred_client.fetch_series() exercising the tenacity retry decorator path
- All 21+ existing backend tests pass without regression
- REQUIREMENTS.md accurately reflects Phase 1 completion (all 15 requirement IDs checked)
- Verification score moves from 19/21 to 21/21
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-ingestion/01-06-SUMMARY.md`
</output>
