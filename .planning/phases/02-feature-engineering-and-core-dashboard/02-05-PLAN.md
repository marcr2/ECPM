---
phase: 02-feature-engineering-and-core-dashboard
plan: 05
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - frontend/src/app/indicators/page.tsx
  - frontend/src/app/indicators/layout.tsx
  - frontend/src/app/indicators/[slug]/page.tsx
  - frontend/src/app/indicators/[slug]/loading.tsx
  - frontend/src/app/indicators/methodology/page.tsx
  - frontend/src/app/indicators/compare/page.tsx
  - frontend/src/components/indicators/indicator-card.tsx
  - frontend/src/components/indicators/methodology-table.tsx
  - frontend/src/components/layout/sidebar.tsx
autonomous: false

requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "User sees the indicator overview page with hero cards (TRPF trio) and secondary cards at /indicators"
    - "User can click any indicator card to navigate to its detail page at /indicators/{slug}"
    - "User sees an interactive time-series chart with Brush zoom, crisis annotations, and date range presets on detail pages"
    - "User can overlay a second indicator with dual y-axes via the overlay selector"
    - "User can toggle between Shaikh/Tonak and Kliman methodologies globally"
    - "User can view methodology documentation with LaTeX formulas and NIPA mappings at /indicators/methodology"
    - "User can view side-by-side methodology comparison at /indicators/compare"
    - "Sidebar shows Indicators section as enabled with sub-links for Overview, individual indicators, and Methodology"
  artifacts:
    - path: "frontend/src/app/indicators/page.tsx"
      provides: "Indicator overview dashboard with hero + secondary cards"
    - path: "frontend/src/app/indicators/[slug]/page.tsx"
      provides: "Individual indicator detail page with interactive chart"
    - path: "frontend/src/app/indicators/methodology/page.tsx"
      provides: "Methodology documentation page with KaTeX formulas"
    - path: "frontend/src/app/indicators/compare/page.tsx"
      provides: "Side-by-side methodology comparison view"
    - path: "frontend/src/components/indicators/indicator-card.tsx"
      provides: "Card component for overview dashboard with sparkline"
    - path: "frontend/src/components/indicators/methodology-table.tsx"
      provides: "Side-by-side diff table comparing methodology NIPA mappings"
    - path: "frontend/src/components/layout/sidebar.tsx"
      provides: "Updated sidebar with Indicators section enabled and sub-navigation"
  key_links:
    - from: "frontend/src/app/indicators/page.tsx"
      to: "frontend/src/lib/indicators.ts"
      via: "fetches indicator overview from API"
      pattern: "fetchIndicatorOverview"
    - from: "frontend/src/app/indicators/[slug]/page.tsx"
      to: "frontend/src/components/indicators/indicator-chart.tsx"
      via: "renders main chart component with data"
      pattern: "<IndicatorChart"
    - from: "frontend/src/app/indicators/methodology/page.tsx"
      to: "frontend/src/components/indicators/formula-display.tsx"
      via: "renders LaTeX formulas from API docs"
      pattern: "<Formula"
    - from: "frontend/src/app/indicators/page.tsx"
      to: "frontend/src/components/indicators/indicator-card.tsx"
      via: "renders overview cards with sparklines"
      pattern: "<IndicatorCard"
---

<objective>
Build all frontend indicator pages: overview dashboard with hero/secondary cards, individual indicator detail pages with interactive charts, methodology documentation page with KaTeX formulas, and side-by-side methodology comparison view. Update the sidebar to enable Indicators navigation.

Purpose: This is the user-facing culmination of Phase 2 -- turning computed Marxist indicators into an interactive, explorable dashboard. The overview provides an "economic pulse at a glance" while detail pages allow deep investigation with zoom, overlays, and crisis context.

Output: 4 new pages (/indicators, /indicators/[slug], /indicators/methodology, /indicators/compare), 2 new components (indicator-card, methodology-table), updated sidebar with sub-navigation.
</objective>

<execution_context>
@/home/marcellinor/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcellinor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feature-engineering-and-core-dashboard/02-CONTEXT.md
@.planning/phases/02-feature-engineering-and-core-dashboard/02-RESEARCH.md
@.planning/phases/02-feature-engineering-and-core-dashboard/02-03-SUMMARY.md
@.planning/phases/02-feature-engineering-and-core-dashboard/02-04-SUMMARY.md

<interfaces>
<!-- From Plan 02-03 (components and data definitions) -->

From frontend/src/lib/indicators.ts:
```typescript
export async function fetchIndicatorOverview(methodology?: string): Promise<IndicatorOverview>
export async function fetchIndicatorData(slug: string, params?: {...}): Promise<IndicatorData>
export async function fetchIndicatorComparison(slug: string): Promise<{...}>
export async function fetchMethodologyDocs(methodologySlug?: string): Promise<MethodologyDocResponse>
```

From frontend/src/lib/indicator-defs.ts:
```typescript
export const INDICATOR_DEFS: IndicatorDef[]
export const HERO_INDICATORS: IndicatorDef[]    // rate-of-profit, occ, rate-of-surplus-value
export const SECONDARY_INDICATORS: IndicatorDef[] // mass-of-profit, productivity-wage-gap, etc.
```

From frontend/src/lib/crisis-episodes.ts:
```typescript
export const CRISIS_EPISODES: CrisisEpisode[]
```

From frontend/src/components/indicators/:
```typescript
export function IndicatorChart(props: { data, primaryKey, overlayKey?, crisisMode, ... })
export function CrisisAnnotations(props: { crises, visible })
export function Sparkline(props: { data, color?, height? })
export function DateRangeControls(props: { onRangeChange, activeRange })
export function OverlaySelector(props: { currentSlug, onOverlayChange, activeOverlay })
export function MethodologyToggle(props: { active, onChange, methodologies })
export function Formula(props: { latex, displayMode? })
```

CRITICAL Next.js 16 notes:
- Dynamic route params are Promises: `const { slug } = await props.params`
- Error boundary uses `unstable_retry` (not `reset`)
- App Router: page.tsx for routes, layout.tsx for shared layout, loading.tsx for Suspense
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build overview dashboard and indicator card component</name>
  <files>
    frontend/src/app/indicators/page.tsx
    frontend/src/app/indicators/layout.tsx
    frontend/src/components/indicators/indicator-card.tsx
    frontend/src/components/layout/sidebar.tsx
  </files>
  <action>
**indicator-card.tsx** -- Overview card component:
- Props: `indicator: IndicatorSummary` (from API), `size: "hero" | "secondary"`, `def: IndicatorDef`
- Hero cards: larger (take up 1/3 of row), prominent sparkline, large latest value, trend badge (rising/falling/flat with colored arrow)
- Secondary cards: smaller, compact sparkline, smaller text
- Uses shadcn/ui Card, CardHeader, CardTitle, CardContent
- Sparkline component from Plan 02-03 for visual trend
- Link wrapping entire card: `<Link href={/indicators/${def.slug}}>`
- Show units next to latest value
- Show latest_date formatted
- `'use client'` (for Link and interactive hover)

**layout.tsx** -- Indicators section layout:
- Wraps all `/indicators/*` pages
- Contains a MethodologyToggle at the top of the content area
- Stores active methodology in URL search params or React state
- Passes methodology context to children (can use React context or URL params)
- Uses the existing layout shell (sidebar + content area)

**page.tsx** -- Indicator overview dashboard (DASH-04):
- Fetches indicator overview from API: `fetchIndicatorOverview(methodology)`
- **Hero row**: 3 large IndicatorCards for TRPF trio (Rate of Profit, OCC, Rate of Surplus Value) per user decision
  - Grid: `grid-cols-3` on lg, `grid-cols-1` on smaller
- **Secondary section**: remaining 5 indicator cards in a responsive grid
  - Grid: `grid-cols-5` on xl, `grid-cols-3` on lg, `grid-cols-2` on md, `grid-cols-1` on sm
- Each card shows: name, latest value with units, trend badge, sparkline
- Cards link to `/indicators/{slug}` detail pages
- Add auto-refresh polling every 60 seconds (consistent with Phase 1 pattern)
- Use try/catch for API fetch with fallback to error state

**sidebar.tsx** -- Update existing sidebar:
- Change Indicators item to `enabled: true`
- When Indicators section is active (pathname starts with /indicators), expand sub-navigation:
  - Overview (/indicators)
  - Individual indicator links (/indicators/{slug}) for each of the 8 indicators
  - Methodology (/indicators/methodology)
  - Compare (/indicators/compare)
- Sub-items styled as indented secondary links under "Indicators"
- Use collapsible section (show sub-nav when parent is active, collapse when not)
- Keep all other sidebar items unchanged
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Indicator overview page renders hero row with 3 TRPF cards and secondary section with 5 cards. Each card shows sparkline, latest value, trend. Cards link to detail pages. Sidebar shows Indicators as enabled with expanded sub-navigation. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build detail page, methodology page, and comparison view</name>
  <files>
    frontend/src/app/indicators/[slug]/page.tsx
    frontend/src/app/indicators/[slug]/loading.tsx
    frontend/src/app/indicators/methodology/page.tsx
    frontend/src/app/indicators/compare/page.tsx
    frontend/src/components/indicators/methodology-table.tsx
  </files>
  <action>
**[slug]/page.tsx** -- Individual indicator detail page (DASH-01, DASH-02, DASH-03):
- Dynamic route: `const { slug } = await props.params` (Next.js 16 async params)
- Fetch indicator data: `fetchIndicatorData(slug, { methodology })`
- Page header: indicator name, units, methodology badge
- Main content: `<IndicatorChart>` with full data, crisis annotations (defaulting to 'shaded' per user decision)
- Controls section below chart:
  - `<DateRangeControls>` for 5Y/10Y/25Y/50Y/All presets
  - `<OverlaySelector>` for dual y-axis overlay ("Add overlay" dropdown)
  - Crisis annotation toggle: shaded / lines / hidden (three-button group)
- When overlay selected: fetch second indicator data, pass both to IndicatorChart with overlayKey/overlayLabel
- Formula display: show the indicator's LaTeX formula below the chart (fetched from methodology docs)
- Handle date range by filtering data client-side based on selected range
- `'use client'` for chart interactions and state management

**[slug]/loading.tsx** -- Loading state for detail page:
- Large skeleton for chart area, smaller skeletons for controls

**methodology/page.tsx** -- Methodology documentation page (DASH-05):
- Fetch methodology docs: `fetchMethodologyDocs()`
- For each methodology, for each indicator:
  - Indicator name heading
  - Formula rendered with `<Formula latex={doc.formula_latex} />`
  - NIPA component mappings table: columns = Marx Category, NIPA Table, Line, Description, Operation
  - Interpretation notes paragraph
  - Citations list
- Per user decision: side-by-side diff tables showing where Shaikh/Tonak and Kliman diverge
- Use the MethodologyTable component (below) for comparison sections
- Section headings with anchor links for each indicator

**compare/page.tsx** -- Side-by-side methodology comparison view:
- Fetch comparison data for a selected indicator: `fetchIndicatorComparison(slug)`
- Dropdown to select which indicator to compare
- Chart showing both methodologies overlaid on same chart using IndicatorChart with dual lines (one per methodology)
- Different colors per methodology (e.g., Shaikh/Tonak = blue, Kliman = red)
- Legend identifying each line by methodology name
- Crisis annotations still visible
- Per user decision: this is the dedicated comparison view

**methodology-table.tsx** -- Side-by-side diff table component:
- Props: `indicator: string`, `shaikhTonak: IndicatorMethodologyDoc`, `kliman: IndicatorMethodologyDoc`
- Two-column table: Shaikh/Tonak column vs. Kliman column
- Rows: Formula, each NIPA mapping, interpretation
- Highlight cells where the two methodologies diverge (different bg color for differing values)
- Uses shadcn/ui Table component
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Detail page renders interactive chart with crisis annotations, Brush zoom, date range presets, overlay selector, and formula display. Methodology page renders LaTeX formulas and NIPA mapping tables for both methodologies. Compare page shows side-by-side methodology comparison chart. All TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 2 indicator dashboard</name>
  <files>frontend/src/app/indicators/page.tsx</files>
  <action>
Visual and functional verification of the complete Phase 2 indicator dashboard. All automation is complete in Tasks 1-2. This checkpoint verifies the full stack works end-to-end.

What was built:
- Overview dashboard with 8 indicator cards (3 hero TRPF trio + 5 secondary) with sparklines and trend badges
- Individual detail pages with interactive Recharts charts (Brush zoom, crisis annotations as shaded regions, dual y-axis overlay, date range presets)
- Methodology documentation page with KaTeX-rendered LaTeX formulas and NIPA mapping tables
- Side-by-side methodology comparison view showing same indicator under Shaikh/Tonak vs. Kliman
- Updated sidebar navigation with Indicators section enabled and sub-links

How to verify:
1. Start the stack: `docker compose up` (or run backend and frontend separately)
2. Navigate to http://localhost:3000/indicators
   - Verify: hero row shows 3 large cards (Rate of Profit, OCC, Rate of Surplus Value) with sparklines and latest values
   - Verify: secondary section shows 5 smaller cards (Mass of Profit, Productivity-Wage Gap, Credit-GDP Gap, Financial-Real Ratio, Debt Service Ratio)
   - Verify: methodology toggle at top switches between Shaikh/Tonak and Kliman
3. Click "Rate of Profit" card to navigate to /indicators/rate-of-profit
   - Verify: interactive chart with time-series data
   - Verify: crisis annotations visible as shaded regions
   - Verify: Brush component at bottom allows zoom/pan
   - Verify: date range presets (5Y, 10Y, 25Y, 50Y, All) filter the data
   - Verify: "Add overlay" dropdown lets you overlay another indicator with dual y-axis
   - Verify: annotation toggle switches between shaded/lines/hidden
4. Navigate to /indicators/methodology
   - Verify: formulas rendered with proper math typography (fractions, subscripts)
   - Verify: NIPA mapping tables show table/line references
   - Verify: side-by-side comparison highlights where Shaikh/Tonak and Kliman diverge
5. Navigate to /indicators/compare
   - Verify: dropdown to select indicator
   - Verify: chart shows both methodology lines overlaid with legend
6. Check sidebar: Indicators section enabled, sub-links visible when expanded
7. Check loading states: navigate to indicator page and verify skeleton placeholders appear briefly
8. Check error state: disconnect backend and verify error boundary shows retry option
  </action>
  <verify>Visual inspection by user following the steps above</verify>
  <done>User has verified the complete Phase 2 dashboard works: overview cards display, detail charts are interactive, methodology docs render correctly, comparison view functions, and sidebar navigation is updated.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd /home/marcellinor/Desktop/ECPM/frontend && npx tsc --noEmit`
- No ESLint errors: `cd /home/marcellinor/Desktop/ECPM/frontend && npx next lint`
- All pages exist: `ls frontend/src/app/indicators/page.tsx frontend/src/app/indicators/[slug]/page.tsx frontend/src/app/indicators/methodology/page.tsx frontend/src/app/indicators/compare/page.tsx`
- Backend tests still pass: `cd /home/marcellinor/Desktop/ECPM/backend && python -m pytest tests/ -x --timeout=30`
</verification>

<success_criteria>
- Overview page shows hero TRPF trio cards + 5 secondary cards with sparklines (DASH-04)
- Detail pages have interactive charts with Brush zoom, crisis annotations, date presets (DASH-01, DASH-03)
- Dual y-axis overlay works on detail pages (DASH-02)
- Methodology page shows LaTeX formulas, NIPA mappings, side-by-side comparison (DASH-05)
- Comparison view shows same indicator under both methodologies
- Sidebar updated with Indicators section and sub-navigation
- Loading/error states work for all indicator pages
- Visual verification confirms the dashboard matches the Bloomberg/Grafana data-dense aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-engineering-and-core-dashboard/02-05-SUMMARY.md`
</output>
