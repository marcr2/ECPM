---
phase: 02-feature-engineering-and-core-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/ecpm/indicators/__init__.py
  - backend/ecpm/indicators/base.py
  - backend/ecpm/indicators/registry.py
  - backend/ecpm/indicators/definitions.py
  - backend/ecpm/indicators/computation.py
  - backend/ecpm/schemas/indicators.py
  - backend/tests/test_indicators.py
  - backend/tests/test_api_indicators.py
  - backend/tests/test_methodology_registry.py
autonomous: true
requirements: [FEAT-01, FEAT-09]

must_haves:
  truths:
    - "MethodologyMapper ABC can be subclassed and registered in the registry"
    - "MethodologyRegistry discovers, stores, and retrieves mappers by slug"
    - "Indicator definitions enumerate all indicators with slugs, display names, and units"
    - "Pydantic response schemas define the API contract for indicator endpoints"
    - "Frequency alignment strategy is explicitly documented as LOCF-only"
  artifacts:
    - path: "backend/ecpm/indicators/base.py"
      provides: "MethodologyMapper ABC with NIPAMapping and IndicatorDoc dataclasses"
      exports: ["MethodologyMapper", "NIPAMapping", "IndicatorDoc"]
    - path: "backend/ecpm/indicators/registry.py"
      provides: "MethodologyRegistry class for mapper discovery"
      exports: ["MethodologyRegistry"]
    - path: "backend/ecpm/indicators/definitions.py"
      provides: "IndicatorSlug enum and INDICATOR_DEFS constant"
      exports: ["IndicatorSlug", "INDICATOR_DEFS"]
    - path: "backend/ecpm/schemas/indicators.py"
      provides: "Pydantic v2 response models for all indicator endpoints"
      exports: ["IndicatorDataPoint", "IndicatorResponse", "IndicatorSummary", "IndicatorOverviewResponse", "NIPAMappingDoc", "IndicatorMethodologyDoc", "MethodologyDocResponse"]
    - path: "backend/tests/test_indicators.py"
      provides: "Test scaffolds for indicator computation"
    - path: "backend/tests/test_api_indicators.py"
      provides: "Test scaffolds for indicator API endpoints"
    - path: "backend/tests/test_methodology_registry.py"
      provides: "Tests for registry plugin pattern"
  key_links:
    - from: "backend/ecpm/indicators/registry.py"
      to: "backend/ecpm/indicators/base.py"
      via: "imports MethodologyMapper ABC"
      pattern: "from ecpm\\.indicators\\.base import MethodologyMapper"
    - from: "backend/ecpm/schemas/indicators.py"
      to: "backend/ecpm/indicators/definitions.py"
      via: "uses IndicatorSlug for validation"
      pattern: "from ecpm\\.indicators\\.definitions import"
---

<objective>
Create the backend interface contracts and test infrastructure for the NIPA-to-Marx translation engine.

Purpose: Establishes the ABC plugin architecture, registry pattern, Pydantic response schemas, indicator definitions, and test scaffolds that all subsequent plans depend on. This is the "contract-first" foundation -- later plans implement against these interfaces.

Output: Backend indicators package with ABC, registry, definitions, schemas, and comprehensive test scaffolds with mock data fixtures.
</objective>

<execution_context>
@/home/marcellinor/.claude/get-shit-done/workflows/execute-plan.md
@/home/marcellinor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-feature-engineering-and-core-dashboard/02-RESEARCH.md

<interfaces>
<!-- Existing backend patterns the executor needs -->

From backend/ecpm/models/observation.py:
```python
class Observation(Base):
    __tablename__ = "observations"
    observation_date: Mapped[dt.datetime]  # primary key
    series_id: Mapped[str]                 # primary key, FK to series_metadata
    value: Mapped[Optional[float]]
    vintage_date: Mapped[Optional[dt.date]]
    gap_flag: Mapped[bool]
    created_at: Mapped[dt.datetime]
```

From backend/ecpm/models/series_metadata.py:
```python
class SeriesMetadata(Base):
    __tablename__ = "series_metadata"
    id: Mapped[int]                         # primary key
    series_id: Mapped[str]                  # unique
    source: Mapped[str]                     # "FRED" or "BEA"
    name: Mapped[str]
    units: Mapped[Optional[str]]
    frequency: Mapped[str]                  # "D", "M", "Q", "A"
    observation_count: Mapped[int]
    fetch_status: Mapped[str]               # pending, ok, error, stale
```

From backend/ecpm/cache.py:
```python
def build_cache_key(endpoint: str, params: dict[str, Any] | None = None) -> str
async def cache_get(key: str, redis: Any = None) -> Optional[str]
async def cache_set(key: str, value: str, ttl: int = 300, redis: Any = None) -> None
```

From backend/ecpm/api/data.py:
```python
def _align_frequency(observations: list[Observation], target_frequency: str) -> list[Observation]
# LOCF alignment -- groups by period, keeps latest per period
```

From backend/ecpm/database.py:
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]
```

Test pattern from backend/tests/conftest.py:
- Uses in-memory SQLite with aiosqlite
- Overrides get_db and lifespan for testing
- _HAS_ECPM_MODELS and _HAS_ECPM_DB flags for conditional setup
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create indicator package with ABC, registry, definitions, and schemas</name>
  <files>
    backend/ecpm/indicators/__init__.py
    backend/ecpm/indicators/base.py
    backend/ecpm/indicators/registry.py
    backend/ecpm/indicators/definitions.py
    backend/ecpm/indicators/computation.py
    backend/ecpm/schemas/indicators.py
  </files>
  <action>
Create the `backend/ecpm/indicators/` package with the following files:

**base.py** -- MethodologyMapper ABC exactly as specified in 02-RESEARCH.md Pattern 1:
- `NIPAMapping` dataclass with fields: marx_category, nipa_table, nipa_line, nipa_description, operation, notes
- `IndicatorDoc` dataclass with fields: name, slug, formula_latex, interpretation, mappings (list[NIPAMapping]), citations (list[str])
- `MethodologyMapper` ABC with abstract methods: name (property), slug (property), compute_rate_of_profit, compute_occ, compute_rate_of_surplus_value, compute_mass_of_profit, get_required_series, get_documentation
- Add abstract methods for remaining indicators: compute_productivity_wage_gap(data, window=20) -> pd.Series, compute_credit_gdp_gap(data) -> pd.Series, compute_financial_real_ratio(data) -> pd.Series, compute_debt_service_ratio(data) -> pd.Series
- All compute methods take `data: dict[str, pd.Series]` and return `pd.Series`
- Note: productivity-wage gap and financial fragility methods can have default implementations (not abstract) since they don't vary by Marxist methodology -- but keep them on the base class for API consistency

**registry.py** -- MethodologyRegistry class exactly as specified in 02-RESEARCH.md Pattern 2:
- Class-level `_mappers: dict[str, MethodologyMapper]` storage
- `register(cls, mapper)`, `get(cls, slug)`, `list_all(cls)`, `default(cls)` class methods
- `default()` returns the "shaikh-tonak" mapper
- Add `reset(cls)` classmethod for test isolation (clears _mappers dict)

**definitions.py** -- Indicator constants:
- `IndicatorSlug` string enum with values: rate_of_profit, occ, rate_of_surplus_value, mass_of_profit, productivity_wage_gap, credit_gdp_gap, financial_real_ratio, debt_service_ratio
- `INDICATOR_DEFS` dict mapping IndicatorSlug to dict with keys: name (str), units (str), category ("core" | "financial"), description (str)
- Core indicators: Rate of Profit (ratio), Organic Composition of Capital (ratio), Rate of Surplus Value (ratio), Mass of Profit (billions USD)
- Financial indicators: Productivity-Wage Gap (index, base=100), Credit-to-GDP Gap (percentage points), Financial-to-Real Asset Ratio (ratio), Corporate Debt Service Ratio (percent)
- `FREQUENCY_STRATEGY` constant = "LOCF" (documents that LOCF is the only alignment strategy per user decision)

**computation.py** -- Stub computation orchestrator:
- `async def compute_indicator(slug: str, methodology_slug: str, db: AsyncSession) -> pd.Series` -- stub that raises NotImplementedError with message "Implementation in Plan 02-02"
- `async def compute_all_summaries(methodology_slug: str, db: AsyncSession) -> list[dict]` -- stub that raises NotImplementedError
- These stubs will be filled in Plan 02-02 when the methodology mappers exist

**schemas/indicators.py** -- Pydantic v2 response models exactly as specified in 02-RESEARCH.md:
- IndicatorDataPoint(date: datetime, value: float)
- IndicatorResponse(slug, name, units, methodology, frequency, data: list[IndicatorDataPoint], latest_value, latest_date)
- IndicatorSummary(slug, name, units, latest_value, latest_date, trend: Optional[str], sparkline: list[float])
- IndicatorOverviewResponse(methodology: str, indicators: list[IndicatorSummary])
- NIPAMappingDoc(marx_category, nipa_table, nipa_line, nipa_description, operation, notes)
- IndicatorMethodologyDoc(indicator_slug, indicator_name, formula_latex, interpretation, mappings: list[NIPAMappingDoc], citations: list[str])
- MethodologyDocResponse(methodology_slug, methodology_name, indicators: list[IndicatorMethodologyDoc])
- All models use `model_config = ConfigDict(from_attributes=True)` where appropriate

**__init__.py** -- Re-export public API:
- from .base import MethodologyMapper, NIPAMapping, IndicatorDoc
- from .registry import MethodologyRegistry
- from .definitions import IndicatorSlug, INDICATOR_DEFS
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM/backend && python -c "from ecpm.indicators import MethodologyMapper, MethodologyRegistry, IndicatorSlug, INDICATOR_DEFS; from ecpm.schemas.indicators import IndicatorResponse, MethodologyDocResponse; print('All imports OK')"</automated>
  </verify>
  <done>All 6 files exist, all types importable, IndicatorSlug has 8 members, INDICATOR_DEFS has 8 entries, MethodologyMapper has all abstract methods, MethodologyRegistry has register/get/list_all/default/reset methods, all Pydantic schemas validate</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffolds with mock data fixtures</name>
  <files>
    backend/tests/test_indicators.py
    backend/tests/test_api_indicators.py
    backend/tests/test_methodology_registry.py
  </files>
  <behavior>
    - test_methodology_registry.py:
      - Test: register a mock mapper, retrieve by slug, verify it's the same instance
      - Test: list_all returns all registered mappers
      - Test: get() with unknown slug raises KeyError
      - Test: default() returns shaikh-tonak mapper (after registration)
      - Test: reset() clears all registered mappers
    - test_indicators.py (stub tests -- will fail until Plan 02-02 implements mappers):
      - Test: rate of profit = S / (C + V) with known values (S=100, C=300, V=200 -> r=0.2)
      - Test: OCC = C / V with known values (C=300, V=200 -> OCC=1.5)
      - Test: rate of surplus value = S / V (S=100, V=200 -> s/v=0.5)
      - Test: mass of profit returns absolute surplus value
      - Test: productivity-wage gap computes ratio of indices over rolling window
      - Test: credit-to-GDP gap applies HP-like trend extraction
      - Test: financial-to-real asset ratio computes correctly
      - Test: frequency alignment uses LOCF for mixed-frequency inputs
    - test_api_indicators.py (stub tests -- will fail until Plan 02-04 implements endpoints):
      - Test: GET /api/indicators/ returns overview with all indicator summaries
      - Test: GET /api/indicators/{slug} returns time-series data
      - Test: GET /api/indicators/methodology returns docs for all methodologies
      - Test: GET /api/indicators/{slug}/compare returns multi-methodology comparison
  </behavior>
  <action>
Create three test files:

**test_methodology_registry.py** -- These tests should PASS immediately (registry is fully implemented in Task 1):
- Create a concrete mock mapper subclass of MethodologyMapper (DummyMapper) that implements all abstract methods with trivial returns (pd.Series([0.0]))
- Use `MethodologyRegistry.reset()` in a fixture to ensure test isolation
- Write all 5 tests from behavior spec above
- All tests use the DummyMapper, not real Shaikh/Tonak

**test_indicators.py** -- These tests should be MARKED with `@pytest.mark.skip(reason="Awaiting Plan 02-02 implementation")`:
- Create a shared fixture `mock_nipa_data` that returns `dict[str, pd.Series]` with known values:
  - "national_income": pd.Series([1000, 1100, 1200], index=pd.date_range("2020", periods=3, freq="YE"))
  - "compensation": pd.Series([600, 650, 700], ...)
  - "corporate_profits": pd.Series([200, 220, 240], ...)
  - "net_fixed_assets_current": pd.Series([3000, 3200, 3400], ...)
  - "net_fixed_assets_historical": pd.Series([2500, 2700, 2900], ...)
  - "output_per_hour": pd.Series([100, 105, 110], ...)
  - "real_compensation_per_hour": pd.Series([100, 101, 102], ...)
  - "credit_total": pd.Series with 40 quarterly values for HP filter testing
  - "nominal_gdp": pd.Series with 40 quarterly values
- Write the 8 indicator computation tests per behavior spec
- Each test imports from ecpm.indicators and calls the compute method on a concrete mapper

**test_api_indicators.py** -- These tests should be MARKED with `@pytest.mark.skip(reason="Awaiting Plan 02-04 API endpoints")`:
- Follow the existing test pattern from backend/tests/test_api.py (TestClient with dependency overrides)
- Write 4 API endpoint tests per behavior spec
- Each asserts response status code and basic response shape

All test files must be importable and skipped tests must be discovered by pytest (not syntax errors).
  </action>
  <verify>
    <automated>cd /home/marcellinor/Desktop/ECPM/backend && python -m pytest tests/test_methodology_registry.py -x --timeout=30 -v</automated>
  </verify>
  <done>test_methodology_registry.py has 5 passing tests. test_indicators.py has 8+ skipped tests discoverable by pytest. test_api_indicators.py has 4+ skipped tests discoverable by pytest. All test files importable without errors.</done>
</task>

</tasks>

<verification>
- All imports work: `python -c "from ecpm.indicators import MethodologyMapper, MethodologyRegistry, IndicatorSlug"`
- Registry tests pass: `pytest tests/test_methodology_registry.py -v`
- All test files discovered: `pytest tests/test_indicators.py tests/test_api_indicators.py --collect-only`
- No existing tests broken: `cd /home/marcellinor/Desktop/ECPM/backend && python -m pytest tests/ -x --timeout=30`
</verification>

<success_criteria>
- MethodologyMapper ABC defines the complete interface for NIPA-to-Marx translation
- MethodologyRegistry supports register/get/list_all/default/reset operations
- IndicatorSlug enumerates all 8 indicators with definitions
- Pydantic schemas define the full API contract for indicator endpoints
- 5 registry tests pass, 12+ indicator/API tests are skipped but discoverable
- Frequency alignment strategy documented as LOCF-only (FEAT-09)
- All existing Phase 1 tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-feature-engineering-and-core-dashboard/02-01-SUMMARY.md`
</output>
